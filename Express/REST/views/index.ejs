<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Posts</title>
    <!-- <link rel="stylesheet" href="Style.css"> -->

    <style>
        /* Resetting default margin and padding */
        body,
        h1,
        p,
        button {
            margin: 0;
            padding: 0;
        }

        /* Basic styling */
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            padding: 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            text-align: center;
            margin-bottom: 20px;
            color: #333;
        }

        .post {
            padding: 10px;
            display: flex;
            align-items: center;
            gap: 20px;
            justify-content: space-between;
            border-bottom: 1px solid #ccc;
        }

        .username {
            font-weight: bold;
            color: #007bff;
            /* Blue color for username */
        }

        .content {
            margin-left: 10px;
            /* Add some space between username and content */
        }

        button {
            background-color: #007bff;
            color: #fff;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
            border-radius: 4px;
            text-decoration: none;
            display: inline-block;
            margin-top: 10px;
        }

        button:hover {
            background-color: #0056b3;
        }

        button a {
            color: inherit;
            text-decoration: none;
        }

        button:focus {
            outline: none;
        }

        .new-post {
            margin-top: 20px;
            display: block;
            text-align: center;
        }
        .btn{
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
    </style>

</head>

<body>
    <div class="container">
        <h1>Facebook Posts</h1>
        <% for (let post of posts) { %>
            <div class="post">
                <p><span class="username">Username:</span> @_<%= post.username %>
                </p>
                <p><span class="content">Content:</span>    
                    <%= post.content %>
                </p>
                <button><a href="/posts/<%= post.id %>">See More Details</a></button>
                <button><a href="/posts/<%= post.id%>/edit">Edit</a></button>

                <form action="/posts/<%= post.id%>?_method=DELETE" method="post">
                    <button type="submit">Delete</button>
                </form>
            </div>
            <% } %>
            <div class="btn">
                <button class="new-post"><a href="http://localhost:3000/posts/new">Create New Post</a></button>
               
                <form action="/posts?_method=DELETE" method="post">
                    <button type="submit">Delete All Posts</button>
                </form> 

            </div>
    </div>
</body>

</html>